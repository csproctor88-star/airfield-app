import { PageHeader } from '@/components/layout/page-header'

// Stub — full implementation in Phase 1 Step 8
export default function NotamsPage() {
  return (
    <div style={{ padding: 16, paddingBottom: 100 }}>
      <PageHeader title="NOTAMs" />
      <div className="card" style={{ textAlign: 'center', padding: 24, color: '#64748B', fontSize: 12 }}>
        NOTAM list — coming in Step 8
      </div>
    </div>
  )
}
